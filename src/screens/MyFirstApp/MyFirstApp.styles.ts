import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
	container: {
		display: 'flex',
		paddingTop: 50,
		gap: 20,
	},
	headerText: {
		fontSize: 20,
		fontWeight: 'bold',
	}
});